apiVersion: v1
kind: Secret
metadata:
  name: bank-transaction-manager-secret
type: Opaque
data:
  # 注意：这些是示例值，实际使用时应使用base64编码的真实值
  datasource_username: c2E=  # sa (base64 encoded)
  datasource_password: cGFzc3dvcmQ=  # password (base64 encoded)